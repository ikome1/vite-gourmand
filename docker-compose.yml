version: '3.8'

services:
  # MongoDB (NoSQL - optionnel)
  mongodb:
    image: mongo:7
    container_name: vite-gourmand-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - vite-gourmand-network
    restart: unless-stopped
    environment:
      - MONGO_INITDB_DATABASE=vite-gourmand

  # Backend API
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: vite-gourmand-server
    ports:
      - "4000:4000"
    volumes:
      - ./server/data:/app/data
      - ./server/src:/app/src
    environment:
      - NODE_ENV=development
      - PORT=4000
      - ALLOWED_ORIGINS=http://localhost:5173
      - MONGODB_URI=mongodb://mongodb:27017
    networks:
      - vite-gourmand-network
    restart: unless-stopped
    command: /app/init.sh
    depends_on:
      - mongodb
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:4000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # Frontend React
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: vite-gourmand-app
    ports:
      - "5173:5173"
    volumes:
      - ./app/src:/app/src
      - ./app/public:/app/public
      - ./app/index.html:/app/index.html
    environment:
      - VITE_API_URL=http://localhost:4000
    depends_on:
      server:
        condition: service_healthy
    networks:
      - vite-gourmand-network
    restart: unless-stopped

networks:
  vite-gourmand-network:
    driver: bridge

volumes:
  mongodb_data:

